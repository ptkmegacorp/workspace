# 2026-03-20

- Identity/user bootstrap established in chat: assistant name set to **Byte** (penguin persona), user identified as **big boy**.
- Workspace repo reset/synced from GitHub backup (`ptkmegacorp/workspace`).
- Created and populated additional local repos:
  - `/home/bot/mothership` (from `ptkmegacorp/mothership`)
  - `/home/bot/sway-dotfiles` (new)
  - `/home/bot/dotfiles` (new)
  - `/home/bot/projects/infernode-docker-dev` (cloned)
- Host/display environment confirmed: Devuan 6 + OpenRC + Sway/Wayland on Hisense Roku TV.
- Major desktop customization completed:
  - Sway tuned for readability and workflow
  - single-workspace workflow enabled
  - swaybar hidden (`mode invisible`)
  - active border vibrant green; inactive/unfocused set to black
  - keybind updates (window kill = `Super+Q`, move window = `Super+Shift+Arrow`)
  - wallpaper changed to flat black (`output * bg #000000 solid_color`)
- Wofi heavily customized to compact/search-bar style and Cascadia font; local app launcher entry for Mothership added.
- Terminal/shell setup changes:
  - zsh installed and set as default shell for `bot`
  - `.bashrc` minimized for zsh-only usage
  - old theme/config restored from prior Debian disk for `.zshrc`/GTK/Xresources
  - foot configured to use zsh and theme aligned to OG Xbox palette
- Audio routing set and persisted to HDMI sink via PulseAudio user config.
- Mothership/OpenRC migration progressed:
  - OpenRC service for mothership installed and started
  - nginx installed/configured, `home.local` resolution and proxy sync working
  - remaining mothership runtime service control paths switched to OpenRC-first/native
- Security hardening applied in strict mode (with caveat):
  - UFW enabled: default deny incoming, allow outgoing
  - inbound allowed from LAN for 22/80/3096 only
  - SSH hardened: root login disabled, password auth disabled, pubkey auth enforced, X11 forwarding disabled
  - unattended-upgrades installed
  - fail2ban install failed on this environment and was removed to keep package state clean
- Browser stack direction changed:
  - Cog installed and set as default app path for Mothership/local app launching
  - old Firefox helper scripts/profiles (`firefox-app`, `firefox-normal`, app-profile dirs) removed
- User preference highlights:
  - prefers OpenRC-native tooling over systemd paths
  - prefers minimal/clutter-free home directory (disabled XDG auto-user dirs)
  - prefers black-heavy OG Xbox visual style
