## Pre-compaction durable memory flush (2026-03-19)

- User wants Devuan migration to be "ready-to-go" on first boot: OpenClaw preinstalled/configured, git ready, key tooling planned (including ollama/llama.cpp), and handoff flow back to agent after boot.
- User explicitly asked to include relevant `/home/bot` state in migration scope, especially XFCE themes/colors/UI config.
- Devuan live ISO downloaded at `/home/bot/projects/devuan-iso/devuan_excalibur_6.1.1_amd64_desktop-live.iso` and SHA256 validated (`d0d5fc8d8e0b980ff4ccc9f2653a5d549f3ee227ac4e01f3d194d0ca2fe85886`).
- Migration bundle scaffold created at `/home/bot/projects/devuan-migration` with bootstrap scripts/manifests for repo sync, XFCE restore, package install, and performance tuning.
- Hardware profile captured: i5-4570, 8GB RAM class, Intel iGPU, previously HDD bottleneck identified; user then confirmed migration target is SSD (same other hardware), implying SSD-oriented tuning should be preferred.
- Remaster pipeline test started; first non-root run failed (device/xattr constraints), then rerun as root was in progress during this session.

## Handoff update (NixOS pivot)
- User pivoted from Devuan migration to full NixOS install request on SSD.
- True state at handoff: NixOS install is NOT complete.
- Confirmed hardware mapping at last checks:
  - /dev/sda = 119.2G SSD target
  - /dev/sdb = 465.8G HDD running system (/)
  - /dev/sdc = 7.5G USB installer media
- NixOS ISO downloaded and hash verified:
  - /home/bot/projects/nixos-iso/nixos-minimal.iso
  - sha256: acdcf8239f64e5acd20cf49c63f83e4c1b823b31d9f669033b48876b29b52177
- NixOS USB write to /dev/sdc completed, but SSD NixOS install not proven.
- Blocking/observed errors during attempted host-side install path:
  - `error: unrecognised flag '-c'`
  - `experimental Nix feature 'nix-command' is disabled`
- Required next-step proof gates for next session:
  1) installer boot
  2) nixos-generate-config --root /mnt
  3) nixos-install
  4) reboot SSD
  5) prove with `nixos-version` + `/etc/os-release` + lsblk
