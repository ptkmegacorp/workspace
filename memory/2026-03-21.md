# 2026-03-21

## Devuan session stack: major progress

- Confirmed successful reboot/login into Sway on canonical stack:
  - OpenRC + dbus + seatd + greetd + Wayland Sway
- Verified live minimal compositor path worked.
- Added formal baseline docs and risk tracking in session-bundle project.

## Selector upgrade completed

- Installed and configured `tuigreet` for greetd session selection.
- Updated greetd flow to:
  - `greetd (_greetd) -> tuigreet -> selected session`
- Added `sway-minimal.desktop` pointing to canonical `/usr/local/bin/sway-session`.
- Reboot/login test succeeded after selector change.
- Promoted selector-enabled state as new known-good baseline (v2) in project docs.

## Git/project outcomes

- Created/pushed new repo:
  - https://github.com/ptkmegacorp/devuan-config-session-bundle
- Finalized cleanup commit in project repo for v2 checklist naming consistency.

## SwayFX project migration + protocolization

- Moved dynamic island project from `/home/bot/projects/sway-agent-topbar-dynamic-island` to `/bot/devuan-config-swayfx`.
- Added full protocol framework in project:
  - `docs/update-protocol.md`, `docs/risk-register.md`, `docs/baseline-v1-checklist.md`, `docs/custom-pipeline-swayfx.md`, `TOC.md`
  - `scripts/verify.sh`, `scripts/capture-live.sh`, `scripts/apply-live.sh`, `scripts/rollback-last.sh`
- Synced live normal-Sway baseline artifacts into `live/` (sway/i3blocks/wofi/foot/kitty/logout scripts).

## SwayFX login-path fixes and validation

- Found and fixed root cause where SwayFX menu selection still launched minimal session:
  - `start-swayfx.sh` and `start-swayfx-safe.sh` were incorrectly forwarding to `/usr/local/bin/sway-session`.
  - Updated them to launch explicit SwayFX configs.
- Confirmed real logout/login into SwayFX works after fix.
- Updated both repos (`devuan-config-session-bundle` and `devuan-config-swayfx`) to reflect corrected invariants and baseline docs.

## Quickshell adoption (SwayFX-only)

- Built and installed Quickshell from source (`/usr/local/bin/quickshell`, symlink `qs`).
- Kept launch scope strict:
  - Normal Sway remains native swaybar path.
  - SwayFX launches Quickshell as primary bar.
- Hid native swaybar in SwayFX via config override (`bar bar-0 { mode invisible }`).

## Dynamic island UI progress

- Reworked center island/nav in Quickshell:
  - Removed apps/term buttons.
  - Added live 24h clock button.
  - Scaled bar/buttons/island ~20% larger.
- Added expanded island chat scaffold:
  - bubble list rendering from state file
  - input box + send button
  - action enqueue path wired from UI

## Composable Telegram architecture decisions

- Locked project to Unix-style contract-first model:
  - UI is pure renderer/actor; no Telegram API logic in QML.
  - Pluggable producer model (OpenClaw adapter now, broker-first later).
- Added `docs/island-io-contract.md` (state + action queue contract):
  - `/run/island/state.json`
  - `/run/island/actions/*.json`
- Added `docs/telegram-integration-spec.md` updates for 2-way chat interaction.
- Added mock/runtime scaffolding:
  - `scripts/island-mock-producer.py`
  - `scripts/island-action-runner.py`
  - `scripts/island-action-enqueue.sh`
  - OpenRC service templates under `ops/openrc/`.
- Added execution planning docs:
  - `docs/island-functionality-checklist.md`
  - `docs/protocol-loop.md`.

## Working-state milestone (late session)

- Rebuilt Quickshell with full Wayland module support after resolving missing protocol/dependency issues.
- Dynamic island hotkey/input is now in a working stable loop inside SwayFX:
  - expand -> type works
  - Esc/send/collapse -> releases focus
  - re-expand -> type works again
- Final focus strategy that worked:
  - `WlrLayershell.keyboardFocus` bound to island state
  - expanded = `Exclusive`, collapsed = `None`
- Added SwayFX reload hardening for false-positive swaynag reappearance on `$mod+Shift+c`.
- Project state is confirmed working and committed/pushed in `devuan-config-swayfx`.
